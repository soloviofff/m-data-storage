# Server
PORT=8080
LOG_LEVEL=info

# Auth
API_TOKEN=changeme
FINALIZATION_WINDOW_HOURS=24

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=m-data-storage
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Timeframes
SUPPORTED_TIMEFRAMES=1m,5m,15m,30m,1h,4h,1d
